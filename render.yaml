services:
  - type: web
    name: sat-constancia-fiscal-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: ./start.sh
    envVars:
      - key: PYTHON_VERSION
        value: 3.10
    plan: free
